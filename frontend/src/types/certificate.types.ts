/** Espelha com.edustream.api.dto.CertificateResponseDTO. */
export interface Certificate {
  id: string;
  studentName: string;
  courseTitle: string;
  generateAt: string;
  validationHash: string;
}
